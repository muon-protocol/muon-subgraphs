{
  "name": "mrc20BridgeRopsten",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ znamazi/mrc20BridgeRopsten",
    "create-local": "graph create --node http://localhost:8020/ znamazi/mrc20BridgeRopsten",
    "remove-local": "graph remove --node http://localhost:8020/ znamazi/mrc20BridgeRopsten",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 znamazi/mrc20BridgeRopsten"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
